<UserControl x:Class="RLFreestyle_v3.MVVM.View.ClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RLFreestyle_v3.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource BackgroundColor}">
        <TextBox Text="{Binding ConsoleOut}" Margin="10,10,10,45" Style="{DynamicResource Area}"/>
        <Button Command="{Binding CloseCommand}" Background="{DynamicResource ButtonBackgroundColor}" Content="DISCONNECT SERVER" Margin="10,0,10,10" Height="30" VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>
